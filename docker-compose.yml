services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: lpdoctor-postgres
    environment:
      POSTGRES_USER: lpdoctor
      POSTGRES_PASSWORD: lpdoctor_dev
      POSTGRES_DB: lpdoctor
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-pgvector.sql:/docker-entrypoint-initdb.d/init-pgvector.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lpdoctor"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: lpdoctor-redis
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
